---
"@nui-kit/react": patch
---

아코디언에서 화살표만 버튼인 모드의 누르는 범위를 44px 로 넓혔다.

`Accordion.Head` 에 `buttonIndex` 를 주면 화살표 아이콘만 버튼이 되는데, 그 버튼은 36px 로 보이고 36px 만 눌렸다. 다른 아이콘 버튼처럼 보이는 크기는 그대로 두고 누르는 범위만 44px 로 넓혔다. 헤더 전체가 버튼인 모드는 원래 넓어서 그대로다.
